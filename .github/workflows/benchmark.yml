name: Run TypeScript Benchmark

on:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Print runner spec
        run: |
          echo "CPU cores:"
          nproc
          echo "Memory:"
          free -h
